# SSL 验证专用配置
user root;

server {
    listen 80;
    server_name www.unocodex.com;

    # Let's Encrypt 验证路径
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # 其他所有请求返回 404
    location / {
        return 404;
    }
} 